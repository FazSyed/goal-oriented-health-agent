;Header and description

(define (domain care_routine)

(:requirements :strips :fluents :durative-actions :timed-initial-literals :typing :conditional-effects :negative-preconditions :duration-inequalities :equality)

(:predicates
    (risk_mild)
)

(:action care_plan_one
    :parameters ()
    :precondition (and )
    :effect (and )
)

(:action care_plan_two
    :parameters ()
    :precondition (and )
    :effect (and )
)

(:action care_plan_three
    :parameters ()
    :precondition (and )
    :effect (and )
)

)